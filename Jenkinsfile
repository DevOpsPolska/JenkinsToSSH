pipeline {
    
    agent any
    environment {
        
    }
    stages {
        stage('copy') {
            steps{
                ssh 
                /*
                withCredentials([]) {
                    //sh ‘ssh user@server mkdir -p /var/www/temp_deploy’
                    
                }*/
                echo 'End of Deployment'
            }
        }
    }
}